Things to do
make a catch for if user makes the wrong pid choice
    send has a catch

Document for Stravation problem 
rn does noting for it

ADD a check for if the messages are no greater than 40;


Document if u allow init to do semaphore

when exiting a process make sure to free the message if it has one


 documentation for more than one message
    If process Receives another message while one is still pending then the most recent message will fail a
    and not send